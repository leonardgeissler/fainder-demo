enabled_tests:
  - expected_form_not_queries

fainder_modes:
  - low_memory
  - full_precision
  - full_recall
  - exact

# Number of times to run each query for more reliable timing measurements
num_runs: 3
